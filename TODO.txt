2. find_contiguous_free(..) remove offsetting.
3. simd_find_flags(..) for finding all flags except staged/uploaded
4. block_size incorrect value: currently holds offset to block, needs size of block

Done:
1. make_free(..) correct args -- done
5. recreate_images(..) should only recreate the images that have been left in an undefined state by the bind -- done
